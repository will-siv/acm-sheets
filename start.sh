#!/bin/bash

# starts update script, which should repeat every 24 hours, unless i just do that through a crontab
# also runs main program, which displays the information
# i might not need this one - if the update script is on crontab then it will just do it without any sleep timer

